# particle_halo mode

`particle_halo` remains useful for diagnostics, comparisons, and experiments that need particle-centric halo movement. It can be valuable when validating gradients or communication behavior against older paths.

For new large runs, prefer `mesh_halo` unless the test specifically targets particle-halo behavior.
