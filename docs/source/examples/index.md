# index

This example is a script-oriented PM++ workflow. Run it from an environment with `pmpp` installed and with hardware matching the example requirements.

- Use deterministic seeds.
- Print compact diagnostics.
- Separate correctness checks from timing.
- Avoid hard-coded local paths.

See the corresponding tutorial pages for step-by-step explanations.
